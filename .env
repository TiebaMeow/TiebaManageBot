APSCHEDULER_AUTOSTART=true
APSCHEDULER_CONFIG={"apscheduler.timezone": "Asia/Shanghai"}

# 监听的主机地址和端口
# 如需本机以外的访问，请将 HOST 设置为 0.0.0.0
HOST=127.0.0.1
PORT=18765

# 指令前缀，可以根据需要进行修改
COMMAND_START=["/", ".", "!", "！", "、"]
# 指令分隔符，建议不要更改
COMMAND_SEP=[" "]

# bot 基础数据使用的 PostgreSQL 主机地址，保持注释则使用 SQLite
# PG_HOST=localhost
# PostgreSQL 端口（一般保持默认）
# PG_PORT=5432
# PostgreSQL 用户名
# PG_USERNAME=your_pg_username
# PostgreSQL 密码
# PG_PASSWORD=your_pg_password
# PostgreSQL 使用的数据库名称
# PG_DB=your_pg_database

# 根据链接自动渲染贴子图片时忽略的用户 ID 列表，多个用户 ID 用逗号分隔
# 示例：IGNORE_USERS=[123456789,987654321]
# 通常用于忽略其他带有贴吧链接的机器人的消息
IGNORE_USERS=[]

# API Token，保持注释则表示不启用认证
#API_TOKEN=your_api_token_here

# 是否启用高级功能（true/false）
# 请确保在启用高级功能前，TiebaScraper 已以 Redis + Stream 模式部署
# https://github.com/TiebaMeow/TiebaScraper
ENABLE_ADDONS=false

# 以下配置仅在启用高级功能时生效
# 通常应与 TiebaScraper 的 Redis 和 PostgreSQL 配置保持一致

# 高级功能使用的 Redis 配置
# Redis 主机地址，Docker Compose 环境下请保持与 TiebaScraper 一致
REDIS_HOST=localhost
# Redis 端口（一般保持默认）
REDIS_PORT=6379
# Redis 用户名（一般无需设置）
#REDIS_USERNAME=your_redis_username
# Redis 密码（一般无需设置）
#REDIS_PASSWORD=your_redis_password
# Redis 使用的数据库编号（一般保持默认）
REDIS_DB=0
# Redis Stream 前缀（请与 TiebaScraper 保持一致）
REDIS_STREAM_PREFIX="scraper:tieba:events"
# Redis 规则更新频道名称（请与 TiebaContentReviewer 保持一致）
REDIS_CHANNEL="reviewer:rules:update"

# 高级功能使用的 PostgreSQL 配置
# PostgreSQL 主机地址，Docker Compose 环境下请保持与 TiebaScraper 一致
ADDON_PG_HOST=localhost
# PostgreSQL 端口（一般保持默认）
ADDON_PG_PORT=5432
# PostgreSQL 用户名
ADDON_PG_USERNAME=your_pg_username
# PostgreSQL 密码
ADDON_PG_PASSWORD=your_pg_password
# PostgreSQL 使用的数据库名称
ADDON_PG_DB=your_pg_database
